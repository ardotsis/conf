FROM debian:bookworm

COPY  . /app

RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y \
    openssh-server \
    ca-certificates \
    sudo \
    curl \
    git \
    man \
    zsh \
    tree \
    fzf

# Simulate run script via pipeline (curl)
CMD ["bash", "-c", "cat $DOTFILES_VOLUME_DIR/test.sh | bash -s -- $INSTALL_SCRIPT_PARAMS"]
